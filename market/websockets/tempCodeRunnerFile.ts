// Print all log messages
            // if (logs.logs) {
            //     console.log("\nLog Messages:");
            //     logs.logs.forEach((log, index) => {
            //         console.log(`${index + 1}. ${log}`);
            //     });
            // }